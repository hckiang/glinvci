doc
Meta
Read-and-delete-me
buildscript.sh

src/*.o
src/*.so
src/*.dll
src/*/*.o
src/*/*.so
src/*/*.dll
.RData
.Rhistory
*.log
.Rd2pdf*
autom4te.cache
tests/testthat/_snaps

# conftest should be deleted automatically by ./configure but just in case...
config.status
conftest*

# Makevars should be ignored because it is always generated by ./configure script
# on Unix. Windows doesn't use ./configure at all, but instead uses a static
# prescribed Makevars.win, which should be included into the source code.
src/Makevars

# Emacs tags
src/TAGS
